<UserControl x:Class="PresentationLayer.UserControls.UCAthleteEvaluations"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.UserControls"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dataGridEvaluations" AutoGenerateColumns="False" IsReadOnly="True" Grid.Row="0" Margin="0,0,0,5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Mark" Binding="{Binding Mark}" Width="*"/>
                <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
        <lvc:CartesianChart x:Name="chartMarks" Grid.Row="1" Margin="0,0,0,5">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Date" LabelFormatter="{Binding DateFormatter}" />
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Mark" MinValue="0" MaxValue="10" />
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
        <TextBlock x:Name="lblStatus" Grid.Row="2" FontSize="14" Foreground="DarkBlue" TextAlignment="Center" Visibility="Collapsed" Margin="0,5,0,0"/>
    </Grid>
</UserControl>