<UserControl x:Class="PresentationLayer.UserControls.UcAddTraining"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.UserControls"
             mc:Ignorable="d" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0A234B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderBrush" Value="#041633"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#0A234B" Grid.Row="0" Padding="15">
            <TextBlock Text="Add Training"
                       Foreground="White"
                       FontSize="26"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Date:" Grid.Row="0" VerticalAlignment="Center" Margin="10"/>
            <DatePicker x:Name="dpDate" Grid.Row="0" Width="206" Height="30" HorizontalAlignment="Center" Margin="10"/>

            <Label Content="Start Time:" Grid.Row="1" VerticalAlignment="Center" Margin="10"/>
            <TextBox x:Name="tbStartTime" Grid.Row="1" Width="206" Height="30" HorizontalAlignment="Center" Margin="10"/>

            <Label Content="End Time:" Grid.Row="2" VerticalAlignment="Center" Margin="10"/>
            <TextBox x:Name="tbEndTime" Grid.Row="2" Width="206" Height="30" HorizontalAlignment="Center" Margin="10"/>

            <Label Content="Team:" Grid.Row="3" VerticalAlignment="Center" Margin="10"/>
            <ComboBox x:Name="cbTeam" Grid.Row="3" Width="206" Height="30" HorizontalAlignment="Center" Margin="10"/>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="2" Margin="10">
            <Button x:Name="btnAdd" Content="Add" Style="{StaticResource ButtonStyle}" Click="btnAdd_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Style="{StaticResource ButtonStyle}" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>

</UserControl>
