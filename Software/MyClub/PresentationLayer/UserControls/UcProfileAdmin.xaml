<UserControl x:Class="PresentationLayer.UserControls.UcProfileAdmin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0A234B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderBrush" Value="#041633"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#0A234B" Grid.Row="0" Padding="15">
            <TextBlock Text="Profile"
               Foreground="White"
               FontSize="26"
               FontWeight="Bold"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>
        </Border>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="10,50,0,20" Grid.Row="1">
            <Label Content="First name:" Grid.Column="0" Grid.Row="0" FontWeight="Bold"/>
            <Label Content="" x:Name="lblFirstName" Grid.Column="1" Grid.Row="0" Margin="2"/>
            <Label Content="Last name:" Grid.Column="0" Grid.Row="1" FontWeight="Bold"/>
            <Label Content="" x:Name="lblLastName" Grid.Column="1" Grid.Row="1" Margin="2"/>
            <Label Content="Birth date:" Grid.Column="0" Grid.Row="2" FontWeight="Bold"/>
            <Label Content="" x:Name="lblBirthDate" Grid.Column="1" Grid.Row="2" Margin="2"/>
            <Label Content="Email:" Grid.Column="0" Grid.Row="3" FontWeight="Bold"/>
            <Label Content="" x:Name="lblEmail" Grid.Column="1" Grid.Row="3" Margin="2"/>
            <Label Content="Role type:" Grid.Column="0" Grid.Row="4" Margin="2" />
            <Label Content="" x:Name="lblRoleType" Grid.Column="1" Grid.Row="4" Margin="2"/>
            <Button x:Name="btnEditProfile" Content="Edit your profile" Style="{StaticResource ButtonStyle}" Width="120" Click="btnEditProfile_Click"/>
            <DataGrid x:Name="dgCoachGrid"
                       Margin="0,50,0,20"

                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                     Height="auto"
                     Width="Auto"
                     Grid.Row="1"
                     FontSize="14"
                     AutoGenerateColumns="False">
                <!-- Prvo sve postojeće kolone -->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="UserID" Binding="{Binding UserID}" Width="80"/>
                    <DataGridTextColumn Header="First name" Binding="{Binding FirstName }" Width="80"/>
                    <DataGridTextColumn Header="Last name" Binding="{Binding LastName }" Width="80"/>
                    <DataGridTextColumn Header="Birth date" Binding="{Binding BirthDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="100"/>
                    <DataGridTextColumn Header="TeamID" Binding="{Binding TeamID}" Width="120"/>
                    <DataGridTextColumn Header="StatusID" Binding="{Binding StatusID}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button x:Name="btnEditCoachProfile" Content="Edit selected coach profile" Style="{StaticResource ButtonStyle}" Width="200" Click="btnEditCoachProfile_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
